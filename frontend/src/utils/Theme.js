import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#664FBA",
    },
    secondary: {
      main: "#F97D7D",
    },
    background: {
      default: "#22272A",
    },
    text: {
      primary: "#ECF3FE",
      secondary: "#bdb3e6",
    },
  },
});

export default theme;
